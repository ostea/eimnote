<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="@android:style/Theme.NoTitleBar"/>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowBackground">@color/color_bg_gray</item>
        <item name="android:colorBackground">@color/color_bg_gray</item>
        <item name="android:textCursorDrawable">@null</item>
        <!--<item name="android:windowAnimationStyle">@style/activity_open_exit</item>-->
    </style>

</resources>
